@model Crud_agenda.Models.AgendaClinica
@{ ViewBag.Title = "Editar Habitación"; }

<div class="row"><div class="col-md-8 offset-md-2">
<div class="card">
    <div class="card-header"><h5 class="mb-0"><i class="bi bi-pencil"></i> Editar Habitación</h5></div>
    <div class="card-body">
        @using (Html.BeginForm("Edit", "AgendaClinica", FormMethod.Post))
        {
            @Html.AntiForgeryToken()
            @Html.HiddenFor(m => m.IdClinica)
            @Html.ValidationSummary(true, "", new { @class = "alert alert-danger" })
            <div class="mb-3">
                @Html.LabelFor(m => m.IdEmpresa, new { @class = "form-label" })
                @Html.DropDownListFor(m => m.IdEmpresa, (SelectList)ViewBag.IdEmpresa, new { @class = "form-select" })
            </div>
            <div class="mb-3">
                @Html.LabelFor(m => m.NombreClinica, new { @class = "form-label" })
                @Html.TextBoxFor(m => m.NombreClinica, new { @class = "form-control" })
                @Html.ValidationMessageFor(m => m.NombreClinica, "", new { @class = "text-danger small" })
            </div>
            <div class="mb-3">
                @Html.LabelFor(m => m.Direccion, new { @class = "form-label" })
                @Html.TextBoxFor(m => m.Direccion, new { @class = "form-control" })
            </div>
            <div class="mb-3 form-check">
                @Html.CheckBoxFor(m => m.Activo, new { @class = "form-check-input" })
                @Html.LabelFor(m => m.Activo, new { @class = "form-check-label" })
            </div>
            <button type="submit" class="btn btn-primary">Guardar</button>
            @Html.ActionLink("Cancelar", "Index", null, new { @class = "btn btn-secondary" })
        }
    </div>
</div></div></div>
