@model Crud_agenda.Models.Paciente
@{ ViewBag.Title = "Nuevo Cliente"; }

<div class="row"><div class="col-lg-10 offset-lg-1">
<div class="card">
    <div class="card-header"><h5 class="mb-0"><i class="bi bi-person-plus"></i> Nuevo Cliente</h5></div>
    <div class="card-body">
        @using (Html.BeginForm("Create", "Pacientes", FormMethod.Post))
        {
            @Html.AntiForgeryToken()
            @Html.ValidationSummary(true, "", new { @class = "alert alert-danger" })
            @Html.Partial("_FormPaciente", Model)
            <button type="submit" class="btn btn-primary"><i class="bi bi-check-circle"></i> Guardar</button>
            @Html.ActionLink("Cancelar", "Index", null, new { @class = "btn btn-secondary" })
        }
    </div>
</div></div></div>
